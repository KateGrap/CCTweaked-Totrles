while turtle.detect("Smooth Stone") do
    for i = 1, 2 do
        turtle.turnRight()
    end
end
repeat 
    turtle.forward()
until turtle.detect("Smooth Stone")
while turtle.detect("Smooth Stone") do
    for i = 1, 2 do
        turtle.turnRight()
    end
end
